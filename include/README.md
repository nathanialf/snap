# include/

C headers, tracked. Same content rules as `src/` — original code only,
no leaked-material derivatives. Headers live here even if the
implementation is still in asm form, so that the eventual matched C
compiles against a stable surface.
